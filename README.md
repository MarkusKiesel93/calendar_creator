# Create ICalendar .ics Files from CSV

This small script creates an .ics File from the given 'events.csv' that can be imported from most Calendar Apps.

## Install with

pip3 install -r requirements.txt